// The abstract class AbsTest implements the Testable interface.
public abstract class AbsTest implements Testable {
    // No need to implement display() method here, because the class is abstract.
}
